{
  "name": "orm-email",
  "version": "1.0.0",
  "description": "A node project to showcase Knex ORM in conjunction with pg to store user emails to a database from a ReactJs basic client.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dropTable": "knex migrate:down",
    "migrate": "knex migrate:latest",
    "seed": "knex seed:run --env development",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Workman",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "knex": "^2.4.2",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
